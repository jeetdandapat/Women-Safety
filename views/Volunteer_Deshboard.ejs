
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Women Safety - Volunteer Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    
</head>
<body>
    <!-- Animated background -->
    <div class="bg-animation"></div>

    <!-- Navbar -->
    <nav class="navbar">
        <div class="logo">
            <i class="fas fa-shield-alt"></i>
            <h1>Women Safety</h1>
        </div>
        <a href="#" class="about-btn">
            <i class="fas fa-circle-info"></i>
            <span>About</span>
        </a>
    </nav>

    <!-- Main Container -->
    <main class="main-container">
        <div class="dashboard">
            <!-- Profile Card -->
            <div class="profile-card">
                <div class="profile-img-container">
                    <img src="https://randomuser.me/api/portraits/men/45.jpg" alt="John Doe" class="profile-img">
                    <div class="online-indicator"></div>
                </div>
                
                <h2 class="profile-name">John Doe</h2>
                
                <div class="status-badge">
                    <div class="status-dot"></div>
                    <span class="status-text">ONLINE</span>
                </div>
                
                <div class="contact-info">
                    <div class="contact-item">
                        <i class="fas fa-envelope"></i>
                        <span>john@example.com</span>
                    </div>
                    <div class="contact-item">
                        <i class="fas fa-phone"></i>
                        <span>+123 456 7890</span>
                    </div>
                </div>
                
                <button class="logout-btn">
                    <i class="fas fa-sign-out-alt"></i>
                    Logout
                </button>
            </div>

            <!-- Main Dashboard -->
            <div class="main-dashboard">
                <div class="dashboard-grid">
                    <!-- Emergency Alert -->
                    <div class="card emergency-card">
                        <div class="emergency-indicator"></div>
                        <div class="card-header">
                            <div class="card-dot" style="background: #ef4444;"></div>
                            <h3 class="card-title">Emergency Alert</h3>
                        </div>
                        
                        <div class="emergency-info">
                            <div class="emergency-info-item">
                                <span class="emergency-label">Name:</span>
                                <span class="emergency-value">XYZ</span>
                            </div>
                            <div class="emergency-info-item">
                                <span class="emergency-label">Message:</span>
                                <span class="emergency-value">Help! I'm near you & feel unsafe.</span>
                            </div>
                            <div class="emergency-info-item">
                                <span class="emergency-label">Distance:</span>
                                <span class="emergency-value" style="font-weight: bold;">100 m</span>
                            </div>
                        </div>
                        
                        <div class="map-placeholder">
                            <div class="map-placeholder-content">
                                <div class="map-icon">
                                    <i></i>
                                </div>
                                <p style="color: #94a3b8; font-style: italic;">Emergency Location Loading...</p>
                            </div>
                        </div>
                        
                        <div class="emergency-time">
                            <span class="time-label">Estimated time to reach:</span>
                            <div class="time-value">4 min</div>
                        </div>
                    </div>

                    <!-- Location Card -->
                    <div class="card location-card">
                        <div class="card-header">
                            <div class="card-dot location-dot"></div>
                            <h3 class="card-title">Volunteer Location</h3>
                        </div>
                        
                        <div class="map-placeholder location-map">
                            <div class="map-placeholder-content">
                                <div class="location-icon">
                                    <i></i>
                                </div>
                                <p style="color: #94a3b8; font-style: italic;">Interactive Map Loading...</p>
                            </div>
                        </div>
                    </div>

                    <!-- Status Card -->
                    <div class="card status-card">
                        <div class="card-header">
                            <div class="card-dot status-dot"></div>
                            <h3 class="card-title">Active Status</h3>
                        </div>
                        
                        <div class="status-info">
                            <div class="status-item">
                                <span class="status-label">Status:</span>
                                <div style="display: flex; align-items: center; gap: 0.5rem;">
                                    <div style="width: 0.5rem; height: 0.5rem; background: #10b981; border-radius: 50%; animation: pulse 2s ease-in-out infinite;"></div>
                                    <span class="status-value">Online</span>
                                </div>
                            </div>
                            <div class="status-item">
                                <span class="status-label">Last Updated:</span>
                                <span class="status-value">5 min ago</span>
                            </div>
                            <div class="status-item">
                                <span class="status-label">Accuracy:</span>
                                <span class="status-value">± 500 m</span>
                            </div>
                        </div>
                        
                        <button class="status-btn">View Active History</button>
                    </div>

                    <!-- Action Buttons -->
                    <div class="actions-container">
                        <div class="actions-grid">
                            <button class="action-btn call-btn">
                                <i class="fas fa-phone"></i>
                                Call Support
                            </button>
                            <button class="action-btn send-btn">
                                <i class="fas fa-paper-plane"></i>
                                Send Status
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Bottom Navigation -->
    <nav class="bottom-nav">
        <div class="bottom-nav-content">
            <button class="nav-btn">
                <i class="fas fa-home"></i>
                <span>Home</span>
            </button>
            <button class="nav-btn logout">
                <i class="fas fa-sign-out-alt"></i>
                <span>Logout</span>
            </button>
        </div>
    </nav>

   
</body>
</html>

<style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #0f172a 0%, #1e1b4b 25%, #581c87 50%, #7c2d92 75%, #be185d 100%);
            color: white;
            min-height: 100vh;
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Animated background elements */
        .bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .bg-animation::before {
            content: '';
            position: absolute;
            top: 25%;
            left: 25%;
            width: 256px;
            height: 256px;
            background: rgba(168, 85, 247, 0.1);
            border-radius: 50%;
            filter: blur(96px);
            animation: pulse 3s ease-in-out infinite;
        }

        .bg-animation::after {
            content: '';
            position: absolute;
            top: 75%;
            right: 25%;
            width: 384px;
            height: 384px;
            background: rgba(236, 72, 153, 0.1);
            border-radius: 50%;
            filter: blur(96px);
            animation: pulse 3s ease-in-out infinite 1s;
        }

        /* Navbar */
        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(15, 23, 42, 0.95);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(168, 85, 247, 0.2);
            padding: 1rem 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 50;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .logo i {
            font-size: 2rem;
            color: #ec4899;
            animation: pulse 2s ease-in-out infinite;
            position: relative;
        }

        .logo i::after {
            content: '';
            position: absolute;
            inset: 0;
            width: 2rem;
            height: 2rem;
            background: rgba(236, 72, 153, 0.2);
            border-radius: 50%;
            animation: ping 1s cubic-bezier(0, 0, 0.2, 1) infinite;
        }

        .logo h1 {
            font-size: 2rem;
            font-weight: bold;
            background: linear-gradient(135deg, #ec4899, #a855f7, #3b82f6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .about-btn {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1rem;
            color: #cbd5e1;
            text-decoration: none;
            border-radius: 0.5rem;
            transition: all 0.3s ease;
            transform: translateY(0);
        }

        .about-btn:hover {
            color: #ec4899;
            background: rgba(236, 72, 153, 0.1);
            transform: translateY(-2px) scale(1.05);
        }

        /* Main container */
        .main-container {
            position: relative;
            z-index: 10;
            padding: 6rem 1rem 8rem;
        }

        .dashboard {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }

        @media (min-width: 1024px) {
            .dashboard {
                grid-template-columns: 1fr 3fr;
            }
        }

        /* Profile Card */
        .profile-card {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.5), rgba(88, 28, 135, 0.3), rgba(29, 78, 216, 0.4));
            backdrop-filter: blur(16px);
            border: 1px solid rgba(168, 85, 247, 0.3);
            border-radius: 1rem;
            padding: 1.5rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
            text-align: center;
            position: relative;
            overflow: hidden;
            height: fit-content;
            order: 1;
        }

        @media (min-width: 1024px) {
            .profile-card {
                order: 1;
            }
        }

        .profile-card::before {
            content: '';
            position: absolute;
            top: -1rem;
            right: -1rem;
            width: 6rem;
            height: 6rem;
            background: linear-gradient(135deg, rgba(236, 72, 153, 0.2), rgba(168, 85, 247, 0.2));
            border-radius: 50%;
            filter: blur(24px);
            animation: pulse 3s ease-in-out infinite;
        }

        .profile-card::after {
            content: '';
            position: absolute;
            bottom: -1rem;
            left: -1rem;
            width: 8rem;
            height: 8rem;
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.2), rgba(168, 85, 247, 0.2));
            border-radius: 50%;
            filter: blur(24px);
            animation: pulse 3s ease-in-out infinite 1s;
        }

        .profile-img-container {
            position: relative;
            display: inline-block;
            margin-bottom: 1rem;
            z-index: 10;
        }

        .profile-img {
            width: 6rem;
            height: 6rem;
            border-radius: 50%;
            border: 4px solid #3b82f6;
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
        }

        .online-indicator {
            position: absolute;
            top: -0.25rem;
            right: -0.25rem;
            width: 1.5rem;
            height: 1.5rem;
            background: #10b981;
            border-radius: 50%;
            border: 2px solid white;
            animation: bounce 1s infinite;
        }

        .profile-name {
            font-size: 1.25rem;
            font-weight: bold;
            color: white;
            margin-bottom: 0.75rem;
            position: relative;
            z-index: 10;
        }

        .status-badge {
            display: inline-flex;
            align-items: center;
            padding: 0.5rem 1rem;
            background: linear-gradient(135deg, rgba(16, 185, 129, 0.2), rgba(5, 150, 105, 0.2));
            border: 1px solid rgba(16, 185, 129, 0.5);
            border-radius: 9999px;
            margin-bottom: 1rem;
            position: relative;
            z-index: 10;
        }

        .status-dot {
            width: 0.5rem;
            height: 0.5rem;
            background: #10b981;
            border-radius: 50%;
            margin-right: 0.5rem;
            animation: pulse 2s ease-in-out infinite;
        }

        .status-text {
            color: #10b981;
            font-weight: 600;
            font-size: 0.875rem;
        }

        .contact-info {
            margin: 1.5rem 0;
            position: relative;
            z-index: 10;
        }

        .contact-item {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            color: #cbd5e1;
            margin: 0.75rem 0;
            font-size: 0.875rem;
            transition: color 0.3s ease;
        }

        .contact-item:hover {
            color: white;
        }

        .logout-btn {
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            padding: 0.75rem 1rem;
            background: linear-gradient(135deg, #ef4444, #ec4899);
            color: white;
            border: none;
            border-radius: 0.5rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
            position: relative;
            z-index: 10;
        }

        .logout-btn:hover {
            background: linear-gradient(135deg, #dc2626, #be185d);
            transform: translateY(-2px) scale(1.05);
        }

        /* Main Dashboard */
        .main-dashboard {
            order: 1;
        }

        @media (min-width: 1024px) {
            .main-dashboard {
                order: 2;
            }
        }

        .dashboard-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }

        @media (min-width: 768px) {
            .dashboard-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        /* Cards */
        .card {
            backdrop-filter: blur(16px);
            border-radius: 1rem;
            padding: 1.5rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
            position: relative;
            overflow: hidden;
        }

        .card-header {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 1rem;
        }

        .card-dot {
            width: 0.75rem;
            height: 0.75rem;
            border-radius: 50%;
            animation: pulse 2s ease-in-out infinite;
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: bold;
            color: white;
        }

        /* Emergency Alert */
        .emergency-card {
            background: linear-gradient(135deg, rgba(127, 29, 29, 0.4), rgba(157, 23, 77, 0.3), rgba(127, 29, 29, 0.4));
            border: 2px solid rgba(239, 68, 68, 0.5);
            grid-column: span 1;
            animation: pulse 3s ease-in-out infinite;
        }

        @media (min-width: 768px) {
            .emergency-card {
                grid-column: span 2;
            }
        }

        .emergency-indicator {
            position: absolute;
            top: 1rem;
            right: 1rem;
            width: 1rem;
            height: 1rem;
            background: #ef4444;
            border-radius: 50%;
            animation: ping 1s cubic-bezier(0, 0, 0.2, 1) infinite;
        }

        .emergency-indicator::after {
            content: '';
            position: absolute;
            inset: 0;
            background: #ef4444;
            border-radius: 50%;
        }

        .emergency-info {
            margin: 0.75rem 0;
        }

        .emergency-info-item {
            display: flex;
            margin: 0.75rem 0;
        }

        .emergency-label {
            font-weight: 600;
            color: #fca5a5;
            width: 5rem;
            flex-shrink: 0;
        }

        .emergency-value {
            color: white;
        }

        .map-placeholder {
            background: rgba(30, 41, 59, 0.5);
            border-radius: 0.75rem;
            height: 10rem;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 1px solid rgba(100, 116, 139, 0.3);
            margin: 1rem 0;
            text-align: center;
        }

        .map-placeholder-content {
            position: relative;
            z-index: 10;
        }

        .map-icon {
            width: 3rem;
            height: 3rem;
            background: rgba(239, 68, 68, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 0.5rem;
        }

        .map-icon i {
            width: 1.5rem;
            height: 1.5rem;
            background: #ef4444;
            border-radius: 50%;
            animation: ping 1s cubic-bezier(0, 0, 0.2, 1) infinite;
        }

        .emergency-time {
            text-align: center;
            margin-top: 1rem;
        }

        .time-label {
            font-weight: 600;
            color: #fca5a5;
        }

        .time-value {
            font-size: 2rem;
            font-weight: bold;
            color: white;
            margin-top: 0.25rem;
        }

        /* Location Card */
        .location-card {
            background: linear-gradient(135deg, rgba(30, 58, 138, 0.4), rgba(67, 56, 202, 0.3), rgba(88, 28, 135, 0.4));
            border: 1px solid rgba(59, 130, 246, 0.3);
        }

        .location-dot {
            background: #3b82f6;
        }

        .location-map {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(168, 85, 247, 0.1));
            position: relative;
        }

        .location-icon {
            width: 4rem;
            height: 4rem;
            background: rgba(59, 130, 246, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 0.75rem;
        }

        .location-icon i {
            width: 2rem;
            height: 2rem;
            background: #3b82f6;
            border-radius: 50%;
            animation: ping 1s cubic-bezier(0, 0, 0.2, 1) infinite;
        }

        /* Status Card */
        .status-card {
            background: linear-gradient(135deg, rgba(6, 78, 59, 0.4), rgba(5, 150, 105, 0.3), rgba(13, 148, 136, 0.4));
            border: 1px solid rgba(16, 185, 129, 0.3);
        }

        .status-dot {
            background: #10b981;
        }

        .status-info {
            margin: 1.5rem 0;
        }

        .status-item {
            display: flex;
            justify-content: space-between;
            margin: 0.75rem 0;
        }

        .status-label {
            font-weight: 600;
            color: #6ee7b7;
        }

        .status-value {
            color: white;
            font-weight: bold;
        }

        .status-btn {
            width: 100%;
            padding: 0.75rem 1rem;
            background: linear-gradient(135deg, #3b82f6, #6366f1);
            color: white;
            border: none;
            border-radius: 0.5rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
        }

        .status-btn:hover {
            background: linear-gradient(135deg, #2563eb, #4f46e5);
            transform: translateY(-2px) scale(1.05);
        }

        /* Action Buttons */
        .actions-container {
            grid-column: span 1;
        }

        @media (min-width: 768px) {
            .actions-container {
                grid-column: span 2;
            }
        }

        .actions-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1rem;
        }

        @media (min-width: 768px) {
            .actions-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        .action-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
            padding: 1rem 1.5rem;
            font-weight: bold;
            font-size: 1.125rem;
            color: white;
            border: none;
            border-radius: 0.75rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
        }

        .action-btn:hover {
            transform: translateY(-2px) scale(1.05);
        }

        .call-btn {
            background: linear-gradient(135deg, #ef4444, #ec4899);
        }

        .call-btn:hover {
            background: linear-gradient(135deg, #dc2626, #be185d);
        }

        .send-btn {
            background: linear-gradient(135deg, #10b981, #059669);
        }

        .send-btn:hover {
            background: linear-gradient(135deg, #059669, #047857);
        }

        /* Bottom Navigation */
        .bottom-nav {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            background: rgba(15, 23, 42, 0.95);
            backdrop-filter: blur(12px);
            border-top: 1px solid rgba(168, 85, 247, 0.2);
            padding: 0.75rem 1.5rem;
            z-index: 50;
            box-shadow: 0 -8px 32px rgba(0, 0, 0, 0.4);
        }

        .bottom-nav-content {
            display: flex;
            justify-content: space-around;
            align-items: center;
            max-width: 24rem;
            margin: 0 auto;
        }

        .nav-btn {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.25rem;
            padding: 0.5rem 1rem;
            color: #cbd5e1;
            background: none;
            border: none;
            border-radius: 0.5rem;
            cursor: pointer;
            transition: all 0.3s ease;
            transform: translateY(0) scale(1);
        }

        .nav-btn:hover {
            color: #ec4899;
            background: rgba(236, 72, 153, 0.1);
            transform: translateY(-2px) scale(1.1);
        }

        .nav-btn i {
            font-size: 1.25rem;
        }

        .nav-btn span {
            font-size: 0.75rem;
            font-weight: 600;
        }

        .nav-btn.logout:hover {
            color: #ef4444;
            background: rgba(239, 68, 68, 0.1);
        }

        /* Animations */
        @keyframes pulse {
            0%, 100% {
                opacity: 1;
            }
            50% {
                opacity: 0.5;
            }
        }

        @keyframes ping {
            75%, 100% {
                transform: scale(2);
                opacity: 0;
            }
        }

        @keyframes bounce {
            0%, 100% {
                transform: translateY(-25%);
                animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
            }
            50% {
                transform: translateY(0);
                animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
            }
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .navbar {
                padding: 0.75rem 1rem;
            }
            
            .logo h1 {
                font-size: 1.5rem;
            }
            
            .main-container {
                padding: 5rem 1rem 6rem;
            }
            
            .emergency-card {
                grid-column: span 1;
            }
            
            .actions-container {
                grid-column: span 1;
            }
        }
    </style>